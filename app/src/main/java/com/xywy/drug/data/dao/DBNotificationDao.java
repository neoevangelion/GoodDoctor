package com.xywy.drug.data.dao;

import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.DaoConfig;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table DBNOTIFICATION.
*/
public class DBNotificationDao extends AbstractDao<DBNotification, Long> {

    public static final String TABLENAME = "DBNOTIFICATION";

    /**
     * Properties of entity DBNotification.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property PersonName = new Property(1, String.class, "personName", false, "PERSON_NAME");
        public final static Property MedicineId = new Property(2, String.class, "medicineId", false, "MEDICINE_ID");
        public final static Property MedicineName = new Property(3, String.class, "medicineName", false, "MEDICINE_NAME");
        public final static Property StartDate = new Property(4, java.util.Date.class, "startDate", false, "START_DATE");
        public final static Property RepeatCount = new Property(5, Integer.class, "repeatCount", false, "REPEAT_COUNT");
        public final static Property NotificationEnabled = new Property(6, Boolean.class, "notificationEnabled", false, "NOTIFICATION_ENABLED");
    };


    public DBNotificationDao(DaoConfig config) {
        super(config);
    }
    
    public DBNotificationDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "'DBNOTIFICATION' (" + //
                "'_id' INTEGER PRIMARY KEY AUTOINCREMENT ," + // 0: id
                "'PERSON_NAME' TEXT," + // 1: personName
                "'MEDICINE_ID' TEXT," + // 2: medicineId
                "'MEDICINE_NAME' TEXT," + // 3: medicineName
                "'START_DATE' INTEGER," + // 4: startDate
                "'REPEAT_COUNT' INTEGER," + // 5: repeatCount
                "'NOTIFICATION_ENABLED' INTEGER);"); // 6: notificationEnabled
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "'DBNOTIFICATION'";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, DBNotification entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String personName = entity.getPersonName();
        if (personName != null) {
            stmt.bindString(2, personName);
        }
 
        String medicineId = entity.getMedicineId();
        if (medicineId != null) {
            stmt.bindString(3, medicineId);
        }
 
        String medicineName = entity.getMedicineName();
        if (medicineName != null) {
            stmt.bindString(4, medicineName);
        }
 
        java.util.Date startDate = entity.getStartDate();
        if (startDate != null) {
            stmt.bindLong(5, startDate.getTime());
        }
 
        Integer repeatCount = entity.getRepeatCount();
        if (repeatCount != null) {
            stmt.bindLong(6, repeatCount);
        }
 
        Boolean notificationEnabled = entity.getNotificationEnabled();
        if (notificationEnabled != null) {
            stmt.bindLong(7, notificationEnabled ? 1l: 0l);
        }
    }

    /** @inheritdoc */
    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    /** @inheritdoc */
    @Override
    public DBNotification readEntity(Cursor cursor, int offset) {
        DBNotification entity = new DBNotification( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // personName
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2), // medicineId
            cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3), // medicineName
            cursor.isNull(offset + 4) ? null : new java.util.Date(cursor.getLong(offset + 4)), // startDate
            cursor.isNull(offset + 5) ? null : cursor.getInt(offset + 5), // repeatCount
            cursor.isNull(offset + 6) ? null : cursor.getShort(offset + 6) != 0 // notificationEnabled
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, DBNotification entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setPersonName(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setMedicineId(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
        entity.setMedicineName(cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3));
        entity.setStartDate(cursor.isNull(offset + 4) ? null : new java.util.Date(cursor.getLong(offset + 4)));
        entity.setRepeatCount(cursor.isNull(offset + 5) ? null : cursor.getInt(offset + 5));
        entity.setNotificationEnabled(cursor.isNull(offset + 6) ? null : cursor.getShort(offset + 6) != 0);
     }
    
    /** @inheritdoc */
    @Override
    protected Long updateKeyAfterInsert(DBNotification entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    /** @inheritdoc */
    @Override
    public Long getKey(DBNotification entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
}
